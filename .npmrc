# Refuse to install on a toolchain that writes an incompatible lockfile.
# npm 10 and npm 11+ produce mutually incompatible layouts for this tree;
# engines pins the boundary and this makes npm enforce it at install time
# rather than letting CI fail later with an opaque "Missing: ... from lock file".
engine-strict=true
