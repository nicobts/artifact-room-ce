// Test stub for the `server-only` package, which throws when imported outside a
// React Server Component. Aliased in vitest.config.ts so server modules
// (lib/artifacts, lib/session) can be unit/integration tested.
export {};
