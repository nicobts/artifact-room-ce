name: CLA

# CLA Assistant Lite: on a contributor's first PR the bot asks for a signature
# (a PR comment with the exact sign phrase) and records it on the
# `cla-signatures` branch. Dormant while the repository is private; MUST be in
# place before the repo goes public (open-core prerequisite — contributions
# need a CLA to be relicensable into the commercial edition).
on:
  issue_comment:
    types: [created]
  pull_request_target:
    types: [opened, closed, synchronize]

permissions:
  actions: write
  contents: write
  pull-requests: write
  statuses: write

jobs:
  cla:
    runs-on: ubuntu-latest
    steps:
      - name: CLA Assistant
        if: (github.event.comment.body == 'recheck' || github.event.comment.body == 'I have read the CLA Document and I hereby sign the CLA') || github.event_name == 'pull_request_target'
        uses: contributor-assistant/github-action@ca4a40a7d1004f18d9960b404b97e5f30a505a08 # v2.6.1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          path-to-signatures: signatures/version1/cla.json
          path-to-document: https://github.com/nicobts/artifact-room-ce/blob/main/CLA.md
          branch: cla-signatures
          allowlist: nicobts,dependabot[bot],renovate[bot],*[bot]
